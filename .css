@import 
url("https://fonts.googleapis.com/css?family=Work+Sans: 400,700,600|Inter:400");

.screen a {
    display: content;
    text-decoration: none;
}

.container-central-horizontal {
    display: flex;
    flex-direction: row;
    justify-content: center;
    pointer-events: none;
    width: 100%;
}

.container-center-horizontal > * {
    flex-shrink: 0;
    pointer-events: auto;
}

input:focus {
    outline: none;
}

.v1-4 {
    align-items: flex-start;
    background-color: var(--neutralwhite);
    display: flex;
    flex-direction: column;
    height: 2744px;
    overflow: hidden;
    width: 1562px;
}
